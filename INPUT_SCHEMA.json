{
  "title": "Instagram Profile Tracker",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "usernames": {
      "title": "Usernames",
      "type": "array",
      "description": "Lista de usernames do Instagram (sem @). Adicione um por linha.",
      "items": { "type": "string" },
      "editor": "stringList",
      "prefill": ["exemplo_user1", "exemplo_user2"]
    },
    "postsLimit": {
      "title": "Quantidade de posts para analisar",
      "type": "integer",
      "description": "Número de posts recentes a coletar por perfil (6–100). Use 24 para um retrato confiável.",
      "prefill": 24,
      "minimum": 6,
      "maximum": 100,
      "editor": "number"
    },
    "useLoginCookies": {
      "title": "Usar cookies de login",
      "type": "boolean",
      "description": "Ative se quiser raspar usando uma sessão logada (pode reduzir rate-limit e mostrar dados extras).",
      "prefill": false,
      "editor": "checkbox"
    },
    "cookies": {
      "title": "Cookies (JSON)",
      "type": "string",
      "description": "Cole um array JSON de cookies da sua sessão do Instagram. Ex.: [{\"name\":\"sessionid\",\"value\":\"...\",\"domain\":\".instagram.com\"}]",
      "editor": "textarea"
    },
    "proxy": {
      "title": "Configuração de Proxy",
      "type": "object",
      "description": "Use o Apify Proxy ou configure um proxy próprio.",
      "editor": "proxy"
    }
  },
  "required": ["usernames"]
}

